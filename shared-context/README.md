# Shared Context

Hermes Legion Commander should write repo graph, prompt preflight, cost estimate, cross-validation, and run ledger artifacts here.

Expected repo graph location:

```text
shared-context/repo-map/
```
